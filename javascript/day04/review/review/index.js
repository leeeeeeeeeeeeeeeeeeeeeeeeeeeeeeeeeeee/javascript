// 함수: 마술상자(입력 -> 가공 -> 결과)

function add(a,b) => {
    return a + b;
};

//문자열 함수(메소드)
const coffee = "americano";
coffee.repeat(3);   //americano americano americano
coffee.split("i");  // ['amer','cano']
